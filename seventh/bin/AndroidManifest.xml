<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.seventh"
    android:versionCode="1"
    android:versionName="1.0" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="21" />
    <permission android:name="android.permissions.READ_CONTACTS"></permission>
    <permission android:name="android.permissions.WRITE_CONTACTS"></permission>
    <permission android:name="android.permissions.WIFI_STATE"></permission>
    <permission android:name="android.permissions.BATTERY_STATE"></permission>
    <permission android:name="android.permissions.Camera"></permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/my_icon72"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".DroidActivity2"
            android:label="@string/app_name">
            <intent-filter>
                <action 
                    android:name="android.intent.action.MAIN" />                
                <category 
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
